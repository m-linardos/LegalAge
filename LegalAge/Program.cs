using System;

namespace LegalAge
{
    class Program
    {
        static void Main(string[] args)
        {
            // can they vote or drink?  How mnay years old?
            // search for ways to subrract 2 dates how to calculate age from date. (no, that's getting too much into the weeds)
            // var age = today.Year - birthDate.Year;




            //First Option:
           
            //DateTime(2000, 8, 15);
            //if (birthDate > today.AddYears(-LegalAge))
            //{
            //    LegalAge--;
            //}


            // Second Option:
            //DateTime(2000, 8, 15);
            //int age = someComplexAlgorithmFromTheWeb();
            //if (bithDate.AddYears(21) >= DateTime.Now)
            //{ }

            //WHAT ABOUT LEAP YEAR
    }
}
